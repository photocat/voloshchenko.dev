import {APP_MAIN_NAVIGATION} from "@/lib/constants";
import SectionWrapper from "../section-wrapper";

const Contact = () => {
    return (
        <SectionWrapper id={APP_MAIN_NAVIGATION[2].title}>
            Contact
        </SectionWrapper>
    );
}

export default Contact;
