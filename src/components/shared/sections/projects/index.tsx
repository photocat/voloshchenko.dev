import { APP_MAIN_NAVIGATION } from "@/lib/constants";
import SectionWrapper from "../section-wrapper";

const Projects = () => {
    return (
        <SectionWrapper id={APP_MAIN_NAVIGATION[0].title}>

        </SectionWrapper>
    )
}

export default Projects;
